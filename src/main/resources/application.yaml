spring:
  application:
    name: exam-result-system
  datasource:
    url: jdbc:mysql://localhost:3306/exam_result_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true